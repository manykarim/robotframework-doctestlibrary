# Tasks: dom-assisted-comparison

- [x] 1.1 DOM walker + `dom_snapshot()` in both adapters; unit tests (keyword invocation shapes, element scope)
- [x] 1.2 WebVisualTest: dom_analysis flow (store/refresh/verdict/context), accept_rendering_only; unit tests (all verdict paths, refresh-on-pass, element scope)
- [x] 1.3 Acceptance: CSS-only vs text change in browser_vrt.robot + selenium coverage; docs; full verification
